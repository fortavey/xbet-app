import React from 'react'
import Question from '../components/Question'

export default function Question7Screen({ route, navigation }) {
  return <Question route={route} navigation={navigation} />
}
